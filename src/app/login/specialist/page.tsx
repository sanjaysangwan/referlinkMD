import { PortalFrame } from "../portal-frame";

export default function SpecialistLogin() {
  return (
    <PortalFrame
      portal="specialist"
      kicker="Specialty care"
      title="Know the moment a referral is on its way."
      copy="Text and phone alerts land when a PCP sends a patient. Three months free for the specialty practice, then $49/month. Primary care never pays."
    />
  );
}
