import { PortalFrame } from "../portal-frame";

export default function SpecialistLogin() {
  return (
    <PortalFrame
      portal="specialist"
      kicker="Specialty care"
      title="Know the moment a referral is on its way."
      copy="Text and phone alerts land when a PCP sends a patient. The inbound pool is charted over time, with privileges that keep clinical review with clinicians."
    />
  );
}
